# Deployment config entrypoint

Main smart-contract deployment profiles are maintained under [`contracts/deployment-config/`](../contracts/deployment-config/).

This root path exists so operators have a stable documentation anchor when reviewing release and deployment materials.
