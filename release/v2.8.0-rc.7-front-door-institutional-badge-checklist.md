# v2.8.0-rc.7 Front-Door Institutional Badge Checklist

## Scope

This checklist governs the v2.8.0-rc.7 coherence-and-presentation hardening cut.
It is additive hardening focused on front-door information architecture and badge governance discipline.

## Why rc.7 (instead of reusing rc.6)

- The repository already records a completed `v2.8.0-rc.5` cut in `CHANGELOG.md`.
- This cut promotes the active RC marker and adds release-provenance artifact naming cleanup and front-door verification-path clarity while preserving validator guardrails.
- A distinct RC identifier avoids provenance ambiguity for operators and external reviewers.

## Acceptance criteria

- Root `README.md` keeps a marker-managed, two-row badge rail generated from `release/badges.json`.
- Trust rail includes: release posture, CI, contracts security, release provenance.
- Orientation rail includes: claim boundary, flagship demo, demo ladder, open-ended RSI demo, doctrine stack.
- Badge links resolve locally and workflow badges map to real `.github/workflows/*.yml` files.
- `demos/README.md` keeps a compact marker-managed badge strip.
- Root README first screen includes a compact high-signal navigation map for flagship/ladder/doctrine/release entry points.
- Release provenance workflow upload artifact naming is tag-generic (`release-provenance-<TAG>`) instead of stale train-specific prefixes.
- README / AGENTS / RELEASES / doctrine / demos release posture remains coherent at `v2.8.0-rc.7`.
- No audited/final/mainnet-safe-by-default claim widening is introduced.

## Smoke checks

Run from repository root:

```bash
python scripts/generate_readme_badges.py --write
python scripts/check_readme_badges.py
python scripts/check_readme_badges.py --check-http-links
python scripts/check_release_surface_posture.py
python scripts/check_demo_links.py
python scripts/check_doctrine_consistency.py
python scripts/check_math_markdown.py
```

## Migration notes

- No runtime or data migration.
- Documentation/process migration only: active RC posture markers move to `v2.8.0-rc.7`.
- Badge-row semantics remain controlled through `release/badges.json` and marker-managed generation.

## Rollback notes

- Revert `release/badges.json`, README/demo badge marker blocks, and release posture docs to prior commit.
- Re-run `python scripts/check_readme_badges.py` and `python scripts/check_release_surface_posture.py` to verify restored coherence.

## Security / provenance implications

- No runtime privilege change.
- Keeps workflow-linked trust badges as operator-visible release trust surfaces.
- Strengthens front-door consistency checks with row-label and duplicate-badge guardrails.

## Claim boundary reminder

This release improves front-door trust/navigation coherence only.
It does **not** claim audited-final deployment, unrestricted autonomy, or broad real-world sovereign completion.
