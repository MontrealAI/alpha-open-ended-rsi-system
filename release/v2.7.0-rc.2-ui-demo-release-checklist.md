# v2.7.0-rc.2 UI + Demo + Doctrine Release Checklist

Release type: **verifiable release candidate** (not audited final deployment)

Date target: **2026-04-22**

## Acceptance criteria

- Flagship demo report remains deterministic and replayable (`--assert` mode).
- Flagship report UI clearly shows wedge rationale, assay flow, PASS/FAIL gates, and claim boundaries.
- Dashboard operator UI includes demo ladder navigation and explicit synthetic-vs-real labeling.
- Doctrine stack remains plain-English first and mathematically canonical in GitHub markdown.
- Release notes and README posture are moved to `v2.7.0-rc.2` without widening claims.

## Demo smoke checks

1. `python3 demos/protocol_smart_contract_correctness_demo/run_demo.py --assert`
2. `python3 demos/adjacent_mandate_reuse_proof_demo/run_demo.py`
3. `python3 demos/adjacent_mandate_reuse_proof_real_v1/07_scripts/calculate_q2_scorecard.py`

## Doctrine and integrity checks

1. `python scripts/check_math_markdown.py`
2. `python scripts/check_doctrine_consistency.py`
3. `python scripts/check_demo_links.py`

## Migration notes

- No schema migrations introduced in this RC.
- No API contract changes were introduced in backend routes.
- No Solidity interface changes were introduced in this RC.

## Rollback notes

- If demo/report rendering introduces regressions, roll back `demos/protocol_smart_contract_correctness_demo/src/report.py` to prior RC state.
- If dashboard UI causes operator confusion, roll back `dashboard/index.html`, `dashboard/styles.css`, and `dashboard/app.js` to prior RC while keeping docs/checklist additions.
- If release docs conflict with tag policy, retain `v2.7.0-rc.1` surfaces and cut a corrected RC tag.

## Provenance expectations

- Generate provenance manifest for `v2.7.0-rc.2` tag.
- Publish `SHA256SUMS`, SBOM, and attestation artifacts for release assets.
- Ensure OpenAPI export is included in provenance bundle.

## This release claims

- A stronger and clearer **synthetic** protocol-correctness wedge demo surface.
- More operator-legible dashboard and release guidance.
- Coherent doctrine and demo ladder links for serious reviewers.

## This release explicitly does not claim

- Audited final security assurance.
- Fully deployed production-final sovereign substrate.
- Proof that the broader 👑 α‑AGI Cybersecurity Sovereign 🔱✨ is already realized.
