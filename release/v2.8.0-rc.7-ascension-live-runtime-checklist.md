# v2.8.0-rc.7 Ascension Live Runtime Checklist

## Acceptance criteria

- `python3 demos/ascension-live-runtime/run_demo.py --assert` passes.
- Required runtime artifacts are emitted under `demos/ascension-live-runtime/out/`.
- Runtime scorecard makes local/devnet-only claim boundary explicit.
- Backend `/ascension/*` endpoints return runtime artifacts when demo has been run.
- Dashboard Ascension tab renders runtime status and artifacts.

## Checks

```bash
python3 demos/ascension-live-runtime/run_demo.py --assert
python backend/scripts/export_openapi.py
pytest -q backend/tests
python scripts/contracts/export_abi.py
```

## Artifact checks

- Confirm `out/events.json` contains machine-readable event stream.
- Confirm `out/validation_attestation.json` includes hash-linked required artifacts.
- Confirm `out/ascension_runtime_scorecard.json` has all 12 organism layers.

## Schema validation checks

- `run_demo.py` validates required fields against `schemas/v2.8/*` Ascension schemas.

## Event/indexing checks

- Runtime event names are deterministic and indexable from `out/events.json`.
- Backend `/ascension/status` exposes `event_count` and layer count.

## Dashboard smoke checks

- Open `dashboard/index.html` with API available at `http://localhost:8000`.
- Verify Ascension Runtime tab shows status, scorecard, jobs, reservoir, and architect panels.

## Migration notes

- Additive only. No database migration required.
- New endpoints are read-only and consume local generated artifacts.

## Rollback notes

- Remove `demos/ascension-live-runtime/` and related docs/schema endpoints if rollback is required.
- Revert backend/dashboard additive Ascension surfaces.

## Claim boundary

This release candidate demonstrates a bounded local/devnet runtime loop only.

## What this release claims

The repository now contains a bounded live-devnet Minimum Viable Ascension Runtime that connects Insight, Nova-Seeds, MARK, Sovereign, Marketplace, AGI Jobs, Agents, Validators, Value Reservoir, Architect, and Archive into a local proof-first economic loop.

## What this release does not claim

- audited final deployment
- mainnet production readiness
- real external-market settlement proof
- unrestricted autonomous operation
- completed α‑AGI Ascension
