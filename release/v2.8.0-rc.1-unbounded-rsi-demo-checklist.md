# v2.8.0-rc.1 Unbounded RSI Demo Release Checklist

## Acceptance criteria

- [ ] `python3 demos/unbounded-rsi-system/run_demo.py --assert` passes and emits required artifacts.
- [ ] Demo README clearly separates demonstrated vs simulated vs unproven boundaries.
- [ ] Demo ladder (`demos/README.md`) includes the accelerating-loop role and cross-links.
- [ ] Root README includes accelerating-loop front-door orientation.
- [ ] Dashboard ladder cards include the accelerating-loop demo link.
- [ ] Release surfaces (`CHANGELOG.md`, `RELEASES.md`, checklist) reflect bounded claim boundaries.

## Demo smoke checks

```bash
python3 demos/protocol_smart_contract_correctness_demo/run_demo.py --assert
python3 demos/unbounded-rsi-system/run_demo.py --assert
python3 demos/adjacent_mandate_reuse_proof_demo/run_demo.py
python3 demos/adjacent_mandate_reuse_proof_real_v1/07_scripts/calculate_q2_scorecard.py
python scripts/check_math_markdown.py
python scripts/check_doctrine_consistency.py
python scripts/check_demo_links.py
```

## Provenance expectations

- `package_manifest.json` and `package_hash.txt` generated for immutable package freeze semantics.
- `provenance_log.json`, `safety_gates.json`, and `governance_ruling.json` generated in the same run.
- `mandate3_selection.json` generated with reproducible bounded-candidate scoring policy and winner trace.
- Board outputs (`board_scorecard.json`, `board_scorecard.md`, `parent_wedge_brief.md`, `report.md`, `report.html`) generated with explicit claim boundaries.
- Demo-local doctrine appendix (`demos/unbounded-rsi-system/DOCTRINE_APPENDIX.md`) remains consistent with canonical repository equations.

## Migration notes

- Adds new demo surface only (`demos/unbounded-rsi-system/`); no architecture migration in contracts/backend/sdk/dashboard stacks.
- Validator expectations now include new ladder path.

## Rollback notes

- Roll back this demo and related docs/checklist/validator changes as one batch if release messaging drifts from proof boundaries.
- Re-run smoke checks after rollback.
- If artifacts for an RC tag were already published, cut a new RC tag instead of mutating published release assets.

## Claims this release makes

- A bounded accelerating-loop proof-of-mechanism can be demonstrated deterministically with explicit governance and safety gates.
- Frozen package reuse can drive adjacent control-vs-treatment uplift and support bounded transfer into a second domain with reduced intervention.

## Claims this release explicitly does NOT make

- unrestricted autonomy
- literal unbounded recursive self-improvement
- fully realized broad sovereign operation
- audited final deployment posture
