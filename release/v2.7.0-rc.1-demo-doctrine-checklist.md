# v2.7.0-rc.1 Demo & Doctrine RC Checklist

## Acceptance criteria

- [ ] Flagship protocol correctness demo remains deterministic and runnable from clean checkout.
- [ ] Demo ladder language is consistent and explicit about synthetic vs real-world proof surfaces.
- [ ] Doctrine stack docs are present and linked from root README.
- [ ] Demo ladder links and role labels validate (`README.md` + `demos/README.md`) through `scripts/check_doctrine_consistency.py`.
- [ ] Canonical GitHub markdown math is validated with `scripts/check_math_markdown.py`.
- [ ] Release posture remains RC-only and does not overclaim external proof.

## Smoke checks

- `python3 demos/protocol_smart_contract_correctness_demo/run_demo.py --assert`
- `python3 demos/adjacent_mandate_reuse_proof_demo/run_demo.py`
- `python3 demos/adjacent_mandate_reuse_proof_real_v1/07_scripts/calculate_q2_scorecard.py`
- `python3 scripts/check_math_markdown.py`
- `python3 scripts/check_doctrine_consistency.py`

## Doctrine links

- `docs/DOCTRINE_STACK.md`
- `docs/THERMODYNAMIC_MODEL.md`
- `docs/NATION_STATE_DOCTRINE.md`
- `docs/DEMO_STRATEGY.md`
- `docs/RELEASE_POSITIONING.md`

## Migration notes

- Public-facing names prefer **Protocol Cybersecurity** labels while legacy **Protocol Assurance** aliases remain for compatibility artifacts.
- Sovereign artifact consumers should prefer `ProtocolCybersecuritySovereign-v1.synthetic.json`; legacy alias files are retained as mirrors.
- Capability-pack consumers should prefer `ProtocolCybersecurityPack-v1`; legacy alias files stay present to avoid breaking existing local scripts.

## Rollback notes

- Roll back docs and release-surface commits first.
- Keep existing v2.6.x verification flow unchanged if rollback is needed.
- If demo naming updates break an external integration, preserve legacy aliases and revert only public copy updates.

## Provenance / artifacts

- Emit provenance manifest and SHA256SUMS under tag `v2.7.0-rc.1`.
- Preserve deterministic naming contract from `RELEASES.md`.
- Include OpenAPI export and ABI export in release artifacts.

## Claims and non-claims

This RC claims:
- deterministic synthetic evidence for the protocol-correctness wedge mechanics,
- improved public/operator legibility,
- stronger doctrine and math consistency.

This RC does **not** claim:
- audited final deployment,
- guaranteed production safety by default,
- proven broader cybersecurity sovereign in real-world conditions.
