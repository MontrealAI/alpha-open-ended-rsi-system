# v2.8.0-rc.4 Front-Door Badge & Release-Surface Coherence Checklist

## Scope

This checklist governs the v2.8.0-rc.4 coherence-and-presentation hardening cut.
It remains an additive RC hardening step and does not widen proof or deployment claims.

## Why rc.4 (instead of reusing rc.3)

- The repository already records a completed `v2.8.0-rc.3` cut in `CHANGELOG.md`.
- This update advances release-surface posture markers and publication docs to a new active target.
- A new RC identifier prevents provenance ambiguity for operators validating front-door posture.

## Acceptance criteria

- Root `README.md` has a marker-managed, two-row badge rail generated from `release/badges.json`.
- Trust rail includes: release posture, CI, contracts security, release provenance.
- Orientation rail includes: claim boundary, flagship demo, demo ladder, open-ended RSI demo, doctrine stack.
- Badge links resolve locally and workflow badges map to real `.github/workflows/*.yml` files.
- `demos/README.md` contains a compact marker-managed badge strip.
- README / AGENTS / RELEASES / doctrine / demos release posture remains coherent at `v2.8.0-rc.4`.
- No audited/final/mainnet-safe-by-default claim widening is introduced.

## Smoke checks

Run from repository root:

```bash
python scripts/generate_readme_badges.py --write
python scripts/check_readme_badges.py
python scripts/check_readme_badges.py --check-http-links
python scripts/check_release_surface_posture.py
python scripts/check_demo_links.py
python scripts/check_doctrine_consistency.py
python scripts/check_math_markdown.py
```

## Migration notes

- No data migration.
- Documentation/process migration only: active RC posture markers are moved to `v2.8.0-rc.4`.
- Badge semantics remain controlled by `release/badges.json` row metadata.

## Rollback notes

- Revert `release/badges.json`, README/demo badge marker blocks, and release posture docs to prior commit.
- Re-run `python scripts/check_readme_badges.py` and `python scripts/check_release_surface_posture.py` to verify restored coherence.

## Security / provenance implications

- No runtime privilege change.
- Preserves workflow-linked trust badges and deterministic posture checks.
- Tightens operator clarity that this is a new RC marker alignment cut.

## Claim boundary reminder

This release improves front-door trust/navigation coherence only.
It does **not** claim audited-final deployment, unrestricted autonomy, or broad real-world sovereign completion.
