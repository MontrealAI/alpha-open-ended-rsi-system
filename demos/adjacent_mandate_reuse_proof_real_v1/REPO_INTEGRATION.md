# Repo integration

Recommended target path in `alpha-nova-seeds`:

```text
demos/adjacent_mandate_reuse_proof_real_v1/
```

Recommended commit message:

```text
Add adjacent-mandate reuse proof real v1 pack
```

Recommended PR title:

```text
Add adjacent-mandate reuse proof real v1 experiment pack
```

Recommended PR description:

```markdown
This PR adds a repo-ready Q2 experiment pack for the first real adjacent-mandate reuse proof.

Included:
- exact mandate specs
- reviewer forms
- freeze manifests
- scorecard templates
- proof-docket templates
- scorecard calculation script

The pack is designed to test whether one completed mandate produces a frozen capability package that materially improves the next adjacent mandate under control conditions.
```
