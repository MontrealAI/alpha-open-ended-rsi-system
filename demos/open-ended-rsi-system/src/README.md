# Demo source helpers

This directory is reserved for reusable helpers for the open-ended RSI demo.
The current v2.8.0-rc.2 implementation keeps logic in `run_demo.py` for single-file deterministic replay.
