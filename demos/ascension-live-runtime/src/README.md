# Ascension runtime source notes

`run_demo.py` currently contains the deterministic orchestration logic for this bounded runtime.
This `src/` folder is reserved for future extraction into moduleized components (event adapters, scoring helpers, and replay validators) while preserving deterministic behavior.
